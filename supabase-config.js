/* Supabase browser configuration. Replace the two placeholder values with your
   project URL and anon key (Project Settings → API). Do not put a service-role
   key in any browser file. See supabase/README.md for the full setup. */
window.CAMPGRIDS_SUPABASE_URL = '';
window.CAMPGRIDS_SUPABASE_ANON_KEY = '';
